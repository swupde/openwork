{{- include "openwork-ee.gateway.validate" . }}
{{- include "openwork-ee.customCa.validate" . }}
{{- $gateway := include "openwork-ee.gateway" . | fromYaml }}
{{- if $gateway.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "openwork-ee.inferenceServiceName" . }}
  labels:
    {{- include "openwork-ee.componentLabels" (dict "root" . "component" "inference") | nindent 4 }}
  {{- with $gateway.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ $gateway.service.type }}
  {{- with $gateway.service.loadBalancerClass }}
  loadBalancerClass: {{ . | quote }}
  {{- end }}
  {{- with $gateway.service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  ports:
    - name: http
      port: {{ $gateway.service.port }}
      targetPort: http
  selector:
    {{- include "openwork-ee.componentSelectorLabels" (dict "root" . "component" "inference") | nindent 4 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "openwork-ee.fullname" . }}-inference
  labels:
    {{- include "openwork-ee.componentLabels" (dict "root" . "component" "inference") | nindent 4 }}
spec:
  replicas: {{ $gateway.replicaCount }}
  selector:
    matchLabels:
      {{- include "openwork-ee.componentSelectorLabels" (dict "root" . "component" "inference") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "openwork-ee.componentSelectorLabels" (dict "root" . "component" "inference") | nindent 8 }}
        {{- with $gateway.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- with $gateway.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.customCa.enabled }}
      volumes:
        {{- include "openwork-ee.customCa.volume" . | nindent 8 }}
      {{- end }}
      containers:
        - name: inference
          image: "{{ $gateway.image.repository }}:{{ include "openwork-ee.imageTag" (dict "root" . "componentTag" $gateway.image.tag) }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ $gateway.containerPort }}
          {{- if .Values.customCa.enabled }}
          volumeMounts:
            {{- include "openwork-ee.customCa.volumeMount" . | nindent 12 }}
          {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "openwork-ee.configName" . }}
            - secretRef:
                name: {{ include "openwork-ee.secretName" . }}
          env:
            - name: PORT
              value: {{ $gateway.containerPort | quote }}
            {{- if .Values.customCa.enabled }}
            {{- include "openwork-ee.customCa.env" . | nindent 12 }}
            {{- end }}
            {{- range $key, $value := $gateway.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- include "openwork-ee.gatewayDeploymentEnv" (dict "root" . "env" $gateway.env) | nindent 12 }}
            {{- include "openwork-ee.gatewayTokenEnv" . | nindent 12 }}
          {{- with $gateway.probes.liveness }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with $gateway.probes.readiness }}
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with $gateway.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end }}
