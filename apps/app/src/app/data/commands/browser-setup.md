---
name: browser-setup
description: Use the built-in OpenWork browser
---

Use browser_tabs to find this conversation's tabs, then browser_open to open https://example.com if needed.
Request browser control for this thread through the browser permission card, then observe the page and report its verified title. This one grant covers navigation, reading and scrolling in the thread's tabs. Clicks, typing and keys require individual confirmation; website-tool invocation and result sharing remain separate approvals.
Explain that Take over pauses browser control and revokes the thread's grant for sign-in; Resume browser permits a fresh access request afterward.
The built-in browser has a separate profile from the user's regular browser. External browser control is unavailable.
If browser tools are unavailable, report that browser control needs the desktop Browser extension enabled and its workspace reloaded.
Keep the response short and action-oriented.
